import { Stack, Typography } from "@mui/material";

const Home = () => {
  return (
    <Stack alignItems="center">
      <Typography>Home Page</Typography>
    </Stack>
  );
};

export default Home;
